apiVersion: v1
kind: Service
metadata:
  name: geoflow-horizon-dash
  namespace: geo119
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30910
  selector:
    app: geoflow
    component: horizon
