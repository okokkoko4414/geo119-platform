apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: geo119-dev

resources:
  - ../base
  - namespace.yaml

patches:
  - path: replicas.yaml
  - path: resources.yaml
  - path: configmap-patch.yaml

images:
  - name: ghcr.io/gstack/geoflow
    newTag: dev
