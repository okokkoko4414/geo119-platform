apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - services.yaml
  - deployments.yaml
  - hpa.yaml
  - pvc.yaml
  - pdb.yaml
  - ingress.yaml

images:
  - name: ghcr.io/gstack/geoflow
    newTag: latest
