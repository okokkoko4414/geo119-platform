apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: geoflow

resources:
  - ../base

patches:
  - path: configmap-patch.yaml

images:
  - name: ghcr.io/gstack/geoflow
    newTag: latest
